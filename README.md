# TypeScript-PostgreSQL-CLI
A command-line interface (CLI) application built with TypeScript to manage products in a PostgreSQL database. The project implements full CRUD operations (Create, Read, Update, Delete)
